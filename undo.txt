Strategy:

A line or curve is formed by dots. 

Each dot has the following attributes:
	color, size ,tool
and every dot is stored in a list, which means the basic unit on the canvas is 
a dot. To group it into a object, I record the lenght of each action and stored it on
the undo

Use the action of mouse to define an object: 
	mouse down, mouse up. => dot
	mouse down, mouse move, mouse up. => curve or line.
	mouse down, mouse move, mouse leave. => curve or line.
Each action contains multiple dots, I store the length of

To differentiate dot, line and curve. I record the mumber of "dots" in each 
I create a undoStack and redoStack to store the user's action on canvas.

In canvas, it 